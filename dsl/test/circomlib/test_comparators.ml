open Core
open Typecheck
open Circomlib.Comparators
module U = Test_utils.Utils

(* let _ = U.test is_zero [] *)

(* let _ = U.test is_equal [is_zero] *)

(* let _ = U.test less_than Circomlib.[Bitify.num2bits; Gates.cnot] *)

(* let _ = U.test greater_than [less_than] *)

(* let _ = U.test leq [less_than] *)

(* let _ = U.test geq [leq] *)
